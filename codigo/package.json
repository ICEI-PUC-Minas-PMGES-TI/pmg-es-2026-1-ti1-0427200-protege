{
  "name": "lab-jsonserver",
  "version": "1.0.0",
  "description": "Aplicação Web com backend baseado no JSON Server",
  "main": "index.js",
  "scripts": {
    "server1": "json-server --watch db/db.json --port 3001",
    "server2": "json-server --watch db/db2.json --port 3002",
    "server3": "json-server --watch ../data/db.json --port 3000",
    "start-all": "concurrently \"npm run server1\" \"npm run server2\" \"npm run server3\""
  },
  "keywords": [],
  "author": "Rommel Vieira Carneiro",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1",
    "json-server": "^0.17.4"
  },
  "devDependencies": {
    "concurrently": "^10.0.3"
  }
}